Installation notes
------------------

This section is primarily for administrators of the system. numactl was installed using Easybuild, build details can be found in ``$EBROOTNUMACTL/easybuild`` with a given module loaded.